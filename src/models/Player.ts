export default interface Player {
  alias: string,
  avatar: string,
  created: Date,
  deleteNextSeason: string,
  disabled: boolean,
  email: string,
  firstName: string,
  lastName: string,
  rookie: boolean
}